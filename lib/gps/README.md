# GPS LIBRARY
De code in deze library is gemaakt voor het makkelijk uitlezen van de gps sensor. Waarbij deze continu wordt uitgelezen vanaf er gestart word.

De data wordt standaard teruggegeven als een GPSData struct voor de makkelijkheid.

## Simuleren
Er zit een kleine simulatie in dat een locatie insteld indien nodig. Dit is enkel voor testing.